These scripts generate Netcdf Markup Language files to aggregate the wicci downscaling hosted by the USGS Center for Integrated Data Analytics. 

tree.txt is a slightly modified ls output.

The two scripts create time series joinExisting aggregations.

The unions create union aggregations that lump together spatio-temporally consistent variables. 